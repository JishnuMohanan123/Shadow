================================================================================
SHADOW1834 - ADMIN ACCOUNT CREDENTIALS
================================================================================

ADMIN USER CREDENTIALS:
----------------------
Username: admin
Password: kJDaW^0jZ&lIK^2g
Email:    admin@shadow1834.local

IMPORTANT: Save these credentials securely!

================================================================================
ADMIN PRIVILEGES & CAPABILITIES
================================================================================

The admin account has FULL ACCESS to all administrative functions:

✓ USER MANAGEMENT:
  - View all registered users with complete details
  - Create new user accounts
  - Edit existing user information (username, email, score, level)
  - Delete user accounts (except your own)
  - Grant/revoke admin privileges to other users
  - Manage user badges and achievements
  - Reset user passwords (through user edit)
  - View user join dates and last login times

✓ ACCESS CONTROL:
  - Full access to /admin panel
  - Admin menu item in navigation bar
  - Protected routes require admin authentication
  - Cannot delete own account (safety feature)

✓ SYSTEM MONITORING:
  - View total user count
  - See administrator count
  - Track user activity and progress
  - Monitor completed modules and scores

================================================================================
HOW TO ACCESS ADMIN PANEL
================================================================================

1. Start the Flask application:
   py app.py

2. Open your browser and go to:
   http://localhost:5000

3. Login with the credentials above:
   Username: admin
   Password: kJDaW^0jZ&lIK^2g

4. After login, you'll see an "ADMIN" link in the navigation menu

5. Click "ADMIN" or navigate to:
   http://localhost:5000/admin

================================================================================
ADMIN PANEL FEATURES
================================================================================

USER TABLE VIEW:
- ID: Unique user identifier
- Username: User's login name
- Email: Contact email
- Score: Total points earned
- Level: Current access level
- Badges: Number of badges earned
- Joined: Registration date
- Last Login: Most recent login time
- Admin: Admin status indicator
- Actions: Edit/Delete buttons

EDIT USER:
- Click "Edit" button on any user
- Modify username, email, score, or level
- Grant/revoke admin privileges via checkbox
- Save changes to update database

DELETE USER:
- Click "Delete" button (confirmation required)
- Removes user and all associated data
- Cannot delete your own account

================================================================================
SECURITY NOTES
================================================================================

✓ Password is 16 characters with mixed case, numbers, and symbols
✓ Password is hashed using bcrypt in database
✓ Admin routes are protected with authentication checks
✓ All admin actions are logged in activity_log table
✓ Account lockout after 5 failed login attempts
✓ Session timeout after 30 minutes of inactivity

================================================================================
DATABASE STRUCTURE
================================================================================

The is_admin field was added to the users table:
- Column: is_admin (INTEGER)
- Values: 0 = regular user, 1 = administrator
- Index: Column 16 in the users table

Migration script: migrate_admin.py
Creation script: create_admin.py
Setup script: set_admin_privileges.py

================================================================================
CHANGING ADMIN PASSWORD
================================================================================

To change the admin password:

Option 1: Through Admin Panel
1. Login as admin
2. Go to /admin
3. Find "admin" user in the table
4. Click "Edit"
5. Enter new password
6. Save changes

Option 2: Run Script Again
Run: py set_admin_privileges.py
This will generate a new password and display it

================================================================================
TROUBLESHOOTING
================================================================================

If you cannot access admin panel:

1. Verify you're logged in as "admin":
   py check_admin.py

2. Check is_admin flag is set:
   Should show: is_admin: 1

3. Test credentials:
   py test_admin_access.py

4. Verify admin routes are loaded:
   Check routes.py imports is_admin function

5. Clear browser cache and cookies
   Then login again

================================================================================
FILES CREATED/MODIFIED
================================================================================

Modified Files:
- app.py (added admin functions and is_admin column)
- routes.py (added admin routes)
- templates/base.html (added admin navigation link)

New Files:
- templates/admin.html (admin panel interface)
- migrate_admin.py (database migration)
- create_admin.py (admin user creation)
- set_admin_privileges.py (set admin privileges)
- check_admin.py (verify admin status)
- test_admin_access.py (test admin functionality)
- ADMIN_CREDENTIALS.txt (this file)

================================================================================
SUPPORT
================================================================================

For issues or questions:
1. Check shadow1834.log for error messages
2. Verify database integrity: py check_admin.py
3. Test admin access: py test_admin_access.py
4. Review routes.py for admin route definitions

================================================================================
END OF DOCUMENT
================================================================================
